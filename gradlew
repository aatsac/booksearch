#!/usr/bin/env sh
# Gradle wrapper stub — regenerate with: gradle wrapper --gradle-version 8.7
# Or open the project in Android Studio and let it sync automatically.
echo "Run './gradlew' via Android Studio or install the Gradle wrapper first."
echo "Android Studio will auto-configure the wrapper on first open."
